# add-graphql-router — Tasks

## 1. Router и сабграфы

- [ ] 1.1 Стаб-сабграф на Go (gqlgen, federation v2): тип с `@key`, поле-ссылка на `User` по id; поднимается в стенде
- [ ] 1.2 Cosmo Router в стенде: композиция схем identity-service + стаб-сабграфа, router стартует с валидным суперграфом
- [ ] 1.3 Маршрут `/graphql` через proxy ведёт в router, а не напрямую в сервис; ext-auth остаётся на этом пути

## 2. Ответы на открытые вопросы

- [ ] 2.1 Аутентификация router → наш сабграф: проверить, доходит ли cookie/контекст, и зафиксировать рабочий вариант (проброс cookie либо доверенные заголовки)
- [ ] 2.2 Federation-запрос через границу: `Order.owner → User` резолвится реально, стаб-сабграф видит `X-Identity-*`
- [ ] 2.3 Проверить поведение при отсутствии сессии: router не должен раскрывать схему или данные анонимному вызывающему

## 3. Выводы

- [ ] 3.1 Записать в архитектурную заметку: механизм аутентификации в сабграфы, проброс контекста, требования к Rails-сабграфам, рекомендация по schema registry
- [ ] 3.2 Если спайк выявил необходимость изменений в сервисе — завести отдельный change со спекой (не править молча)
