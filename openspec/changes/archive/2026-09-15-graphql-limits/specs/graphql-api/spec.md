# graphql-api — delta

## MODIFIED Requirements

### Requirement: GraphQL endpoint с аутентификацией по сессии
Сервис SHALL обслуживать GraphQL-запросы на `POST /graphql`, аутентифицируя вызывающего по браузерной cookie-сессии. Запрос без действительной сессии SHALL получать ошибку с кодом `UNAUTHENTICATED` без каких-либо данных. Introspection и сам endpoint MUST NOT раскрывать данные неаутентифицированным вызывающим. Размер тела запроса SHALL быть ограничен, а сложность запроса SHALL оцениваться до исполнения с учётом запрошенных размеров списков; запрос сверх бюджета SHALL отклоняться ошибкой без обращения к данным.

#### Scenario: Запрос без сессии
- **WHEN** приходит GraphQL-запрос без cookie или с недействительной сессией
- **THEN** ответ содержит ошибку `UNAUTHENTICATED` и не содержит данных

#### Scenario: Запрос с действующей сессией
- **WHEN** аутентифицированный пользователь запрашивает `me`
- **THEN** ответ содержит его глобальный ID, email, имя и эффективные права

#### Scenario: Слишком большое тело
- **WHEN** тело запроса превышает установленный предел
- **THEN** запрос отклоняется ошибкой транспорта, резолверы не выполняются

#### Scenario: Запрос сверх бюджета сложности
- **WHEN** аутентифицированный пользователь отправляет запрос, чья расчётная сложность (с учётом размеров списков и алиасов) превышает бюджет
- **THEN** ответ содержит ошибку о превышении сложности и не содержит данных; обращений к хранилищу не происходит

### Requirement: Справочник пользователей и приложений
Аутентифицированный пользователь SHALL иметь возможность запрашивать: список активных пользователей с поиском по имени или email постранично (размер страницы `first` с верхним пределом и непрозрачный курсор `after`, ответ содержит узлы и признак наличия следующей страницы), отдельного пользователя по ID, список приложений с их ролями и permissions. Деактивированные пользователи SHALL попадать в выдачу только по явному запросу (для истории и аудита). Назначения ролей для всех пользователей страницы SHALL загружаться одной выборкой.

#### Scenario: Поиск пользователей
- **WHEN** запрошен `users(search: "ann")`
- **THEN** возвращаются активные пользователи, чьё имя или email содержит подстроку без учёта регистра

#### Scenario: Справочник приложений
- **WHEN** запрошены `applications` с ролями и permissions
- **THEN** возвращается полный справочник, у ролей виден их состав

#### Scenario: Постраничный обход каталога
- **WHEN** каталог содержит больше пользователей, чем `first`, и клиент запрашивает страницы, передавая `endCursor` в `after`
- **THEN** страницы не пересекаются и не пропускают пользователей, последняя страница имеет `hasNextPage: false`

#### Scenario: Недопустимый размер страницы или курсор
- **WHEN** `first` вне допустимого диапазона или `after` не является курсором, выданным сервисом
- **THEN** ответ содержит ошибку `BAD_USER_INPUT`

#### Scenario: Роли страницы одной выборкой
- **WHEN** запрошена страница пользователей с полем `roles`
- **THEN** назначения всех пользователей страницы получены одним запросом к хранилищу
