# cache-user-lookups — Tasks

## 1. Кеш

- [x] 1.1 `identity.ActiveChecker` → `UserCache`: хранит `*User`, методы `FindByID` и `IsActive` из одной записи, отрицательный результат кешируется; unit-тесты со считающим хранилищем: N вызовов → 1 обращение, после TTL → новое, неизвестный пользователь не долбит БД
- [x] 1.2 Wiring: `userSource.FindByID` через кеш; метрики кеша сохраняются
- [x] 1.3 Тест уровня хендлеров: validate обращается к источнику пользователя ровно один раз за запрос (защита от возврата лишнего похода)

## 2. Завершение

- [x] 2.1 `mise run lint`, `mise run test`, `mise run vuln` чистые; живая проверка: два подряд validate → один SELECT в логе запросов PostgreSQL
- [x] 2.2 Синхронизировать спеку, заархивировать, запушить; CI зелёный
