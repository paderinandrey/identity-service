# validate-any-method — Tasks

- [x] 1.1 Снять привязку метода и точного пути с маршрута validate в `internal/session/handlers.go` (обслуживать и поддерево)
- [x] 1.2 Тесты: разные методы и путь с дописанным суффиксом дают одинаковый результат (200 с заголовками при валидной сессии, 401 без неё)
- [x] 1.3 `mise run lint`, `mise run test` чистые; проверить на стенде, что `/graphql` через proxy проходит
