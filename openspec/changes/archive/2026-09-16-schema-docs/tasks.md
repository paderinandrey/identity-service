# schema-docs — Tasks

- [x] 1.1 `tbls` в mise, `.tbls.yml`, таски `schema:docs` / `schema:check`
- [x] 1.2 Сгенерировать и закоммитить `docs/schema/`
- [x] 1.3 CI-джоб `schema-docs` (postgres → migrate → `tbls diff`); README
