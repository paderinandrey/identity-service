# e2e-login

## Purpose

Программная выдача браузерной сессии для E2E-автотестов фронтов: машинный токен вместо интерактивного SSO, полная совместимость выданной сессии с обычной, гарантированная недоступность в production.

## Requirements

### Requirement: Выдача сессии по машинному токену
При заданном `E2E_LOGIN_TOKEN` сервис SHALL обслуживать `POST /internal/e2e/login`, принимающий Bearer-токен и email существующего активного пользователя, и SHALL создавать для него штатную сессию (ротация идентификатора, лимиты, сроки — как при SSO-входе), возвращая session cookie. Сравнение токена SHALL выполняться за константное время.

#### Scenario: Успешная выдача
- **WHEN** запрос с верным токеном и email активного пользователя
- **THEN** ответ содержит Set-Cookie штатной сессии; `GET /auth/me` с этой cookie возвращает пользователя

#### Scenario: Неверный токен
- **WHEN** токен отсутствует или не совпадает
- **THEN** ответ 401 без создания сессии

#### Scenario: Неизвестный или неактивный пользователь
- **WHEN** email не найден или пользователь деактивирован
- **THEN** сессия не создаётся, ответ содержит отказ

### Requirement: Отсутствие следов в аналитике входов
E2E-вход MUST NOT обновлять `last_sign_in_at` и MUST NOT учитываться в метриках SSO-входов.

#### Scenario: Метрики не затронуты
- **WHEN** выдана e2e-сессия
- **THEN** счётчик SSO-входов и `last_sign_in_at` пользователя не изменились

### Requirement: Недоступность в production
Без заданного `E2E_LOGIN_TOKEN` маршрут MUST NOT существовать (404). Конфигурация с `APP_ENV=production` и заданным токеном SHALL быть ошибкой старта.

#### Scenario: Токен не задан
- **WHEN** `E2E_LOGIN_TOKEN` отсутствует
- **THEN** `POST /internal/e2e/login` отвечает 404

#### Scenario: Токен в production
- **WHEN** сервис стартует с `APP_ENV=production` и заданным `E2E_LOGIN_TOKEN`
- **THEN** процесс завершается с ошибкой конфигурации
