extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.3", import: ["@key"])

type Order @key(fields: "id") {
  id: ID!
  title: String!
  owner: User!
  seenIdentityHeaders: [String!]!
}

type User @key(fields: "id", resolvable: false) {
  id: ID!
}

type Query {
  orders: [Order!]!
}
